var username = "peacefulflash";
console.log(username.length);

//  length of the string

var username = "SaydaShamer";
console.log(username.len); // So, we need to use full word to get the length of the string.

var username = "SaydaShamer";
console.log(username.length);