
// we use flower brackets { } for object

var user = {

    name:"Chaitainya"
}
console.log(user);

// For multiple variables, instead of creating many variables. We can use Object.

var user = {
    username: "Chaitainya",
    password: "Chaitu123",
    email: "chaitainyaa@gmail.com"
}

console.log(user);

// To print individuals

console.log(user.username);
console.log(user.password);
console.log(user.email);