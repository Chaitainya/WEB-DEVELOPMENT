// Merging (Concatenating) Arrays
// The concat() method creates a new array by merging (concatenating) existing arrays:

var myFrnds = ["Jack", "Evon", "Ryan", "Leon"];

var herFrnds = ["Chandini", "Vasu", "Priyanka"];

totFrnds = myFrnds.concat(herFrnds);

console.log(totFrnds);