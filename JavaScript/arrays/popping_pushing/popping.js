// Popping

// The pop() method returns the value that was "popped out":

var bikes = ["R1", "FZ 1", "BMW", "Ducati"];

console.log(bikes.pop()); // Ducati

// By the default, pop() will popout the last element.