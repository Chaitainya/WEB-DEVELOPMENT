// The join() method also joins all array elements into a string.

// It behaves just like toString(), but in addition you can specify the separator:

var games = ["GTA V", "RES 4", "POP 5"];

console.log(games.join(" * "));