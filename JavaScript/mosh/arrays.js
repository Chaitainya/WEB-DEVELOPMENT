// let somthing = []; --> (Array Literal) this is called empty array

let selectedColors = ['red', 'blue'];
console.log(selectedColors);

// If we wanna access an element in array we use index, every element in a array has a indexing number.

let selectedColor = ['red', 'blue']; //[0,1,.....]
console.log(selectedColors[0]);

// Adding element to the array

let selectedColors = ['red', 'blue'];
selectedColors[2] = 'green';
console.log(selectedColors);